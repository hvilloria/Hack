.row
  %p#notice= notice
  .col-sm-3
    - @people.each do |person|
      = link_to (button_tag "#{person.name} #{person.lastname}", type: "button",
        :class => "btn btn-primary prueba"), person_path(person)
      -# = image_tag person.photo.url(:medium)

  .col-sm-6.position-button

    -# -#
    -# = link_to (button_tag "Eliminar Chalequeo ", type: "button",
    -#   :class => "btn btn-primary"), new_person_path

  .col-sm-3
