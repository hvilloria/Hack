<h1>Lista de Contactos</h1>
<p>
	<h3>Buscar</h3>
		<%= form_tag("/", method: "get") do %>
			<%= label_tag(:name, "nombre")%>
			<%= text_field_tag(:name) %>
			<%= submit_tag("Buscar") %>
		<% end %>
</p>

<ol>
	<% @contacts.each do |contact| %>
		<li><%= contact.name %></li>
	<% end %>
</ol>
